<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head', { title: 'My Storage Drive' }) %>
</head>
<body>
    <nav class="navbar">
        <div class="container">
            <div class="nav-brand">
                <h2>📁 Storage Drive</h2>
            </div>
            <div class="nav-user">
                <span>Welcome, <strong><%= user.username %></strong></span>
                <form action="/auth/logout" method="POST" style="display: inline;">
                    <button type="submit" class="btn btn-secondary">Log Out</button>
                </form>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="drive-content">
            <h1>My Files</h1>
            
            <%- include('../partials/_messages') %>
            
            <!-- Create Folder Section -->
            <div class="actions-section">
                <div class="create-folder">
                    <h3>Create New Folder</h3>
                    <form action="/mydrive/folders/create" method="POST" class="folder-form">
                        <input 
                            type="text" 
                            name="folderName" 
                            placeholder="Folder name..." 
                            required 
                            maxlength="50"
                        />
                        <button type="submit" class="btn btn-primary">Create Folder</button>
                    </form>
                </div>

                <!-- Upload File Section -->
                <div class="upload-file">
                    <h3>Upload New File</h3>
                    <form action="/mydrive/upload" method="POST" enctype="multipart/form-data" class="upload-form">
                        <input type="file" name="file" required />
                        <button type="submit" class="btn btn-primary">Upload</button>
                    </form>
                </div>
            </div>

            <!-- Folders List -->
            <% if (folders && folders.length > 0) { %>
                <div class="folders-section">
                    <h3>📁 Folders (<%= folders.length %>)</h3>
                    <div class="folders-grid">
                        <% folders.forEach(folder => { %>
                            <div class="folder-card">
                                <div class="folder-icon">📁</div>
                                <div class="folder-name"><%= folder.name %></div>
                                <div class="folder-meta">
                                    <span><%= folder._count ? folder._count.files : 0 %> files</span>
                                </div>
                                <div class="folder-actions">
                                    <a href="/mydrive/folders/<%= folder.id %>" class="btn btn-sm btn-secondary">Open</a>
                                    <form action="/mydrive/folders/<%= folder.id %>/delete" method="POST" style="display: inline;">
                                        <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Delete this folder?')">Delete</button>
                                    </form>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                </div>
            <% } %>

            <!-- Files List -->
            <% if (files && files.length > 0) { %>
                <div class="files-list">
                    <h3>📄 Files (<%= files.length %>)</h3>
                    <table class="files-table">
                        <thead>
                            <tr>
                                <th>Type</th>
                                <th>Name</th>
                                <th>Size</th>
                                <th>Uploaded</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% files.forEach(file => { %>
                                <tr>
                                    <td>
                                        <% if (file.mimeType.startsWith('image/')) { %>
                                            🖼️
                                        <% } else if (file.mimeType === 'application/pdf') { %>
                                            📄
                                        <% } else if (file.mimeType.includes('text')) { %>
                                            📝
                                        <% } else { %>
                                            📎
                                        <% } %>
                                    </td>
                                    <td><%= file.name %></td>
                                    <td>
                                        <% if (file.size < 1024) { %>
                                            <%= file.size %> B
                                        <% } else if (file.size < 1024 * 1024) { %>
                                            <%= (file.size / 1024).toFixed(2) %> KB
                                        <% } else { %>
                                            <%= (file.size / (1024 * 1024)).toFixed(2) %> MB
                                        <% } %>
                                    </td>
                                    <td>
                                        <%= new Date(file.createdAt).toLocaleDateString('en-US', { 
                                            year: 'numeric', 
                                            month: 'short', 
                                            day: 'numeric' 
                                        }) %>
                                    </td>
                                    <td>
                                        <a href="/mydrive/files/<%= file.id %>/download" class="btn btn-secondary btn-sm">
                                            Download
                                        </a>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            <% } %>

            <!-- Empty State -->
            <% if ((!folders || folders.length === 0) && (!files || files.length === 0)) { %>
                <div class="empty-state">
                    <p>📂 Your drive is empty</p>
                    <p class="text-muted">Start by creating a folder or uploading a file!</p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>